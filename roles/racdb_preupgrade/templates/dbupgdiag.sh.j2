source ~/.bash_profile
export ORACLE_SID={{ oracle_sid }}
export ORACLE_HOME={{ oracle_home }}
export PATH=$PATH:$ORACLE_HOME/bin

cd {{ scripts_dir }}
sqlplus -s sys as sysdba @dbupgdiag.sql {{ logdir }}
